# PCCP Implementation Blockers

Blockers encountered during plan execution, with workaround/decision and resolution status.

| # | Date | Sub-project | Blocker | Decision / Workaround | Status |
|---|------|-------------|---------|----------------------|--------|
| 1 | 2026-08-15 | S2 (all later suites) | Pre-existing flake: `internal/audit` chain tests failed under `-count=N` because the chain allocator caches per-org high-water state process-wide, and fixed org IDs leaked previous runs' chains into fresh DBs; SQLITE_BUSY retries also burned allocator sequences. | Fixed in tests: unique org per run + `_busy_timeout`/WAL DSN. Verified 10× green. | Resolved |
| 2 | 2026-08-15 | all suites | Two timing flakes under full-suite parallel load: `internal/sandbox` TestDockerRuntimeWhenAvailable (Docker daemon pull contention) and `internal/scheduler` TestDispatcherStreamingFallsBackToPlainForwarder (2s streaming window under CPU contention). Both pass in isolation and on repeated runs; environment-dependent, not code-path defects. | Left as-is; re-run isolated when they fail. | Open (environmental) |
| 3 | 2026-08-15 | web/25-A, web/26-C partial | Operator SSO (OIDC/SAML) console login: the `sso` endpoints exist and callbacks verify, but issuing a console JWT from an SSO assertion requires the operators/authz plan (the login spec itself defers to it). | Login page shows an honest notice; email/password + TOTP MFA path is the enforced console auth today. | Open (needs operators/authz plan) |
| 4 | 2026-08-15 | web/12-A1, web/24-B | Payment provider integration: `PaymentProvider` is a bare field; no external PSP is wired, so invoices/refunds cannot execute against a real provider. | Refund/credit requests are recorded as audited adjustments with `provider_action: not_configured` — never faked. | Open (needs PSP choice + credentials) |
| 5 | 2026-08-15 | web/22-B, web/19-A (harness side) | Harness-reported feature telemetry + code-level span emission: PCCP records what the harness sends (relay ingestion exists), but the harness client (external patty-code-pccp repo) must emit the records. | Relay-side ingestion/enforcement is complete; external repo change tracked. | Open (external repo) |
| 6 | 2026-08-15 | web/18-A (PIA side) | PIA-side model package verification at load: CP-side publish verification (manifest digest + signature) is enforced; the PIA loader's verification is a cross-repo change. | CP refuses publishing tampered packages; PIA-side check tracked for the PIA repo. | Open (external repo) |
